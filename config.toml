# baseURL = "https://serene-mcclintock-1d41a9.netlify.app/"
baseURL = "https://mdp-graphics.com/"
# baseURL = "https://mdp-graphics.com/hugo-test/"
title = "Printable Wall Art | Printable Planners| MDP Graphics"
theme = "hugo-universal-theme"

languageCode = "en-us"
# Site language. Available translations in the theme's `/i18n` directory.
defaultContentLanguage = "en"
# Enable comments by entering your Disqus shortname
disqusShortname = "mdp-graphics"
# Enable Google Analytics by entering your tracking code
googleAnalytics = "G-YFM5QHEQDN"


# Define the number of posts per page
paginate = 30

[menu]

# Main menu



[[menu.main]]
    identifier = "shop"
    name = "Shop"
    url  = "/printable/"
    weight = 2

[[menu.main]]
    identifier = "free"
    name = "Free"
    url  = "/free/printable/"
    weight = 3

[[menu.main]]
    identifier="blog"
    name = "Blog"
    url  = "/blog/"
    weight = 4

[[menu.main]]
    identifier="about"
    name = "About"
    url  = "/about/"
    weight = 5

[[menu.main]]
    identifier = "faq"
    name = "FAQ"
    url  = "/faq/"
    weight = 6

[[menu.main]]
    identifier = "contact"
    name = "Contact"
    url  = "/contact/"
    weight = 7



# Top bar social links menu

[[menu.topbar]]
    weight = 1
    name = "GitHub"
    url = "https://github.com/devcows/hugo-universal-theme"
    pre = "<i class='fab fa-2x fa-github'></i>"

[[menu.topbar]]
    weight = 2
    name = "Facebook"
    url = "https://www.facebook.com/MDP-Graphics-101265492081322"
    pre = "<i class='fab fa-2x fa-facebook'></i>"



[[menu.topbar]]
    weight = 4
    name = "Email"
    url = "martin@mdp-graphics.com"
    pre = "<i class='fas fa-2x fa-envelope'></i>"

[params]
    viewMorePostLink = "/blog/"
    author = "MDP Graphics"
    defaultKeywords = ["printable wall art", "printable planners"]
    mainSections = ["blog"]
    defaultDescription = "MDP Graphics is your one-stop shop for printable wall art and printable planners. Our wall art printables come in high quality PDF PNG JPEG and sizes small up to large poster. Our planners come in PDF, all sizes."

    # Social media
    facebook_site = "https://www.facebook.com/MDP-Graphics-101265492081322" # the Facebook handle of your site ('https://www.facebook.com/HANDLE')
    # twitter_site = "" # the Twitter handle of your site (without the '@')
    # default_sharing_image = "img/logo.png"

    # Google Maps widget: If `googleMapsApiKey` is not set, no key will be passed to Google (which likely results in a broken map widget).
    enableGoogleMaps = false
    googleMapsApiKey = "AIzaSyCFhtWLJcE30xOAjcbSFi-0fnoVmQZPb1Y"

    latitude = "-12.043333"
    longitude = "-77.028333"

    # Style options: default (light-blue), blue, greenSpringGlam, green, marsala, pink, red, turquoise, violet, greenSpring, lightPink
    style = "greenSpringGlam"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 50 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - register your account to https://formspree.io/register
    # - login and create new form
    # - set your form's endpoint url under 'formspree_action' below
    # - upload the generated site to your server
    # - test a dummy email yourself
    # - you're done. Happy mailing!
    #
    # Enable the contact form by entering your Formspree.io endpoint url
    formspree_action = "https://formspree.io/f/mqkwgggv"
    contact_form_ajax = false

   about_us = "<p>I create high quality, all sizes, printable wall art downloads for homeowners, just download, get printed and framed. Are you looking to decorate your home, but don't know where to start?. With extensive tips, tricks and tutorials, I can help you create a more stylish and tranquil home in no time.</p>"
    copyright = "Copyright (c) 2021, MDP Graphics; all rights reserved."

    # Format dates with Go's time formatting
    date_format = "January 2, 2006"

    logo = "img/logo"
    logo_small = "img/logo-small"
    address = """<p class="text-uppercase"><strong>MDP Graphics</strong>
        <br>Dublin<br>
        <strong>Ireland</strong>
      </p>
      """



[permalinks]
    blog ="/:filename/"





# Enable or disable top bar with social icons
[params.topbar]
    enable = true
    text = "<p>FREE DESIGN - 30% OFF - </p>"

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = true
    categoriesTT = true
    categoriesResources = true

    tags = true
    search = false

[params.carouselCustomers]
    items = 6
    auto_play = false
    slide_speed = 2000
    pagination_speed = 1000

[params.carouselTestimonals]
    items = 4
    auto_play = false
    slide_speed = 2000
    pagination_speed = 1000

[params.carouselHomepage]
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more informtion take a look at the README.
    enable = true
    auto_play = true
    slide_speed = 2000
    pagination_speed = 1000

[params.features]
    enable = true
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more informtion take a look at the README.

[params.testimonials]
    enable = true
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more informtion take a look at the README.
    title = "Testimonials"
    subtitle = "Here is what some of our happy customers had to say."

[params.see_more]
    enable = true
    icon = "fas fa-shopping-cart"
    title = "Browse our shop"
    subtitle = "All our designs come in high quality PNG/JPEG/PDF with high DPI for accurate printing."
    link_url = "printable/"
    link_text = "Shop Now"

[params.clients]
    enable = false
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more informtion take a look at the README.
    title = "Our Clients"
    subtitle = ""

[params.recent_posts]
    enable = true
    title = "From our blog"
    subtitle = "Here are some of our most recent blog posts, be sure to check them out and subscribe to our email list so that you don't miss out on any of our new posts."
    hide_summary = false

 [taxonomies]
  tag = "tags" 
  category = "categories"
 
